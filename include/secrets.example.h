#pragma once
// TEMPLATE — copy this file to include/secrets.h and fill in real values.
// secrets.h is gitignored so real credentials never get committed.

// WiFi (static network tried before the config portal)
#define SECRET_WIFI_SSID  "your-wifi-ssid"
#define SECRET_WIFI_PASS  "your-wifi-password"

// Server HTTP Basic auth (push + relay API)
#define SECRET_PG_USER    "your-api-user"
#define SECRET_PG_PASS    "your-api-password"
